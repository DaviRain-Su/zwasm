# Official corpus category: "refers to resources not defined in the current
# component" (resources.wast, ~5 cases). The verdict is GREEN today but for a
# decode-level reason: resource-type DEFINITIONS (0x3f) are still undecoded
# (UnsupportedTypeForm — D-322), so the PARENT's `(type $t (resource ...))`
# rejects before the alias rule is ever consulted. When D-322 lands resource
# defs, the inner-component outer-alias-of-parent-resource rule (Binary.md:
# outer-aliased types must not be generative) must land WITH it or this case
# regresses to a false accept.
assert_invalid test/spec/component-model-assert/nested_component_resource_refs/outer_alias_resource.wasm
