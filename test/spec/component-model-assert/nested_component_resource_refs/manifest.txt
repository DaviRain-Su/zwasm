# Official corpus category: "refers to resources not defined in the current
# component" (resources.wast, ~5 cases). Representative fixture committed as
# outer_alias_resource.{wat,wasm} for when the gap closes.
skip-impl nested inline-component decode not implemented — the decoder enumerates top-level sections only, so an inner component's outer-alias of a parent resource type is invisible to the validator (ADR-0176 deferred)
