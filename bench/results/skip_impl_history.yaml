# Skip-impl one-way ratchet history — ADR-0050 amend (D-3 / D-4).
#
# Append one row per chunk-close commit. `scripts/check_skip_impl_ratchet.sh`
# (skeleton at this seed; completed in §9.12-A) reads this file at pre-push
# time and FAILs the push if the current commit's skip-impl count is
# strictly greater than the most recent committed row, unless an `exempt`
# field cites an Accepted ADR.
#
# Schema (per row):
#   commit:               git SHA of the chunk-close commit (or "(baseline)")
#   timestamp:            ISO-8601 (UTC)
#   non_simd_skip_impl:   integer; `spec_assert_runner_non_simd` skip-impl
#   simd_skip_impl:       integer; `simd_assert_runner` skip-impl
#   total:                non_simd_skip_impl + simd_skip_impl
#   note:                 short human note (optional)
#   exempt:               ADR-NNNN slug if this row legitimately increases
#                         the count (must reference an Accepted ADR)
#
# Phase 9 completion master plan §7.3 + ADR-0050 (Amend; D-3 / D-4 pending
# §9.12 collab Accept).

entries:
  - commit: "(baseline)"
    timestamp: "2026-05-19T00:00:00Z"
    non_simd_skip_impl: 193
    simd_skip_impl: 50
    total: 243
    note: |
      Seed baseline at §9.12-A enforcement layer landing. Live measurement
      via `zig build test-spec-wasm-2.0-assert` and `zig build test-spec-simd`.
      Breakdown captured in `private/notes/p9-close-skip-impl-inventory.md`:
        - SKIP-CROSS-MODULE-IMPORTS: 100 modules + ~66 cascade
        - SKIP-NO-LINK-TYPECHECK: 26
        - SKIP-VALIDATOR-GAP (SIMD): 50
        - exports/manifest.txt skip-impl: 1
      Phase 9 completion exit criterion = drain this to 0 at §9.12-E (master plan
      §5.3).
    exempt: null

  - commit: "0e353955"
    timestamp: "2026-05-20T20:30:00Z"
    non_simd_skip_impl: 193
    simd_skip_impl: 0
    total: 193
    note: |
      §9.12-E first measurement post-B134. SKIP-VALIDATOR-GAP (SIMD)
      fully discharged (50 → 0) via B133 (lane-index range check) + B134
      (alignment-immediate range check). `simd_assert_runner: 13351
      passed, 0 failed, 0 skip-impl + 390 skip-adr`.
      Remaining: SKIP-CROSS-MODULE-IMPORTS 100, SKIP-NO-LINK-TYPECHECK 26,
      exports/manifest.txt 1, plus ~66 cascade.
    exempt: null

  - commit: "B137-pending"
    timestamp: "2026-05-20T20:50:00Z"
    non_simd_skip_impl: 192
    simd_skip_impl: 0
    total: 192
    note: |
      §9.12-E / B137 — manifest format extension + base runner support
      for `get-action <field> <type> <value>` directive.
      regen_spec_2_0_assert.sh now emits the directive instead of
      `skip-impl non-invoke-action` for same-module `(get "field")`
      actions; base runner recognizes the directive + nullable
      `handle_get_action` callback. Non-SIMD callback stays null
      pending D-152 (compileWasm empty-fn-path globals_offsets fix);
      fallback emits SKIP-NON-INVOKE-ACTION → skipped_adr. Net:
      `spec_assert_runner_non_simd: 25325 passed, 0 failed, 688
      skipped (= 192 skip-impl + 496 skip-adr)`.
    exempt: null

  - commit: "close-plan §6 (e) — semantic shift baseline"
    timestamp: "2026-05-21T00:00:00Z"
    non_simd_skip_impl: 0
    simd_skip_impl: 0
    total: 0
    note: |
      AssertTally split: `skipped` → `manifest_skip_impl` (rename) +
      new `runtime_skip` field (close-plan §6 (e), tracks C2). The
      "skip-impl" counter now strictly counts manifest `skip-impl`
      lines; the prior 192 was almost entirely runtime SKIP-*
      events (SKIP-CROSS-MODULE-IMPORTS×100 + SKIP-NO-LINK-TYPECHECK
      + cascades) which now flow to `runtime_skip` separately.
      Output format extended to 3-counter shape:
      `661 skipped (= 0 skip-impl + 192 runtime-skip + 469 skip-adr)`.
      Forward ratchet semantics: only `manifest_skip_impl` is gated;
      `runtime_skip` is reported but not ratcheted (its growth
      reflects discovery of structural gaps, NOT a release-gate
      regression). Pre-existing baseline entries above are
      historical snapshots under the prior conflated semantics —
      they are NOT re-extracted because we can't re-run those
      commits.
    exempt: null
